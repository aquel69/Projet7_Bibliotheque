<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://localhost:5432/OC-bibliotheque</property>
        <property name="connection.username">postgres</property>
        <property name="connection.password">biblio</property>

        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="connection.pool_size">1</property>

        <!-- Select our SQL dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQL94Dialect</property>

        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>
        <property name="generate_statistics">true</property>
        <property name="format_sql">true</property>

        <!-- Set the current session context -->
        <property name="current_session_context_class">thread</property>

        <!--<mapping class="fr.lardon.bibliocataloguelivres.model.AbonnePret" />
        <mapping class="fr.lardon.bibliocataloguelivres.model.Auteur" />
        <mapping class="fr.lardon.bibliocataloguelivres.model.Editeur" />
        <mapping class="fr.lardon.bibliocataloguelivres.model.Genre" />
        <mapping class="fr.lardon.bibliocataloguelivres.model.Livre" />
        <mapping class="fr.lardon.bibliocataloguelivres.model.Ouvrage" />
        <mapping class="fr.lardon.bibliocataloguelivres.model.Pret" />-->
    </session-factory>

</hibernate-configuration>